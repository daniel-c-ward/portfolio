---
import PageLayout from "../layouts/PageLayout.astro";
import ProjectCard from "../components/ProjectCard.astro";
import { entriesFromGlob, type MdModule } from "../lib/md-entries";

const modules = import.meta.glob<MdModule>("../content/lab/**/*.md", {
  eager: true,
});
const items = entriesFromGlob(modules, "lab");
---

<PageLayout
  title="Lab | Daniel Ward"
  description="Notes, experiments, and prototypes from Daniel Ward."
>
  <div class="page">
    <header class="page-hero">
      <p class="eyebrow">Lab</p>
      <h1>Lab</h1>
      <p class="lead">
        Notes, prototypes, and experiments from <code>src/content/lab</code>.
      </p>
    </header>

    {
      items.length === 0 ? (
        <p class="lead">
          No lab entries yet. Add Markdown files under{" "}
          <code>src/content/lab</code>.
        </p>
      ) : (
        <section class="lab-collection" aria-label="Lab projects">
          {items.map((item, index) => (
            <ProjectCard
              item={item}
              basePath="/lab"
              isCompact={true}
              index={index}
              inCarousel={false}
              actionLabel="View Section"
              href={`/lab/${item.section}`}
            />
          ))}
        </section>
      )
    }
  </div>
</PageLayout>
